# Search Service
Full-text product search and filtering.

## Endpoints
- `GET /search?q=&category=&minPrice=&maxPrice=&minRating=&sort=` — Search products
- `GET /filters` — Get filter options
- `POST /search/log` — Log search query
- `GET /health` — Health check
